# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Dario Gartmann
email: hello@dariogartmann.ch
description: > # this means to ignore newlines until "baseurl:"
  I'm Dario Gartmann. A video creator and photographer based in Switzerland.
  This portfolio showcases some of my work.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://dariogartmann.ch" # the base hostname & protocol for your site, e.g. http://example.com

version: 1.2.1

# Build settings
markdown: kramdown
highlighter: rouge

sass:
  sass_dir: _sass
  style: compressed

collections:
  projects:
    output: true
  traveljournals:
    output: true

permalink: pretty

gems:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
